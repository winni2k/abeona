[tox]
envlist = py36

[testenv]
passenv = *
deps =
    coverage
    pytest
    bitstring
    hypothesis
commands =
    python setup.py --quiet clean develop
    coverage run --parallel-mode -m pytest
    coverage combine --append
    coverage report -m
